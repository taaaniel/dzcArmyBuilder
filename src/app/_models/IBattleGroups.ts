export interface IBattleGroups {
  total: {
    min: number;
    max: number;
  };
  hq: {
    min: number;
    max: number;
  };
  armor: {
    min: number;
    max: number;
  };
  infantry: {
    min: number;
    max: number;
  };
  special: {
    min: number;
    max: number;
  };
  fleet: {
    min: number;
    max: number;
  };

}
